import React from "react";
import { Link } from "react-router";

type Props = {
	name: string
}

const HomePage = (props: Props) => {
	return (
		<div>
			<h1>IN-CORE v3 Template</h1>

			<h2>Get Started</h2>
			<ol>
			</ol>
		</div>
	);
};

export default HomePage;
